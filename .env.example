PUBLIC_SITE_URL=https://toyb.space

# Server-only secrets (never expose with PUBLIC_ prefix)
SUPABASE_URL=
SUPABASE_ANON_KEY=
RESEND_API_KEY=
WAITLIST_FROM="Toyb <hello@toyb.space>"
WAITLIST_IP_SALT=
WAITLIST_TURNSTILE_ENABLED=false

# Turnstile (optional hardening)
# TURNSTILE_SITE_KEY is public and used client-side
TURNSTILE_SITE_KEY=
# TURNSTILE_SECRET_KEY is server-only
TURNSTILE_SECRET_KEY=

# Optional provider override: resend (default) | smtp
WAITLIST_EMAIL_PROVIDER=resend

# Optional local/dev SMTP fallback (Node runtime only)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
