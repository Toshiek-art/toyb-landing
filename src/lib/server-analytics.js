/**
 * Server-side analytics hook.
 *
 * Tracking is intentionally disabled for now.
 * Wire this to your provider in one place when needed.
 */
export function trackServerEvent(event, payload) {
  void event;
  void payload;
}
